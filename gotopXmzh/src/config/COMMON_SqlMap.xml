<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN" "http://ibatis.apache.org/dtd/sql-map-2.dtd">
<sqlMap namespace="COMMON">
  <sql id="abatorgenerated_Example_Where_Clause">
    <!--
      WARNING - This element is automatically generated by Abator for iBATIS, do not modify.
      This element was generated on Wed Nov 02 10:12:45 CST 2011.
    -->
    <iterate conjunction="or" prepend="where" property="oredCriteria" removeFirstPrepend="iterate">
      <isEqual compareValue="true" property="oredCriteria[].valid">
        (
        <iterate conjunction="and" prepend="and" property="oredCriteria[].criteriaWithoutValue">
          $oredCriteria[].criteriaWithoutValue[]$
        </iterate>
        <iterate conjunction="and" prepend="and" property="oredCriteria[].criteriaWithSingleValue">
          $oredCriteria[].criteriaWithSingleValue[].condition$
            #oredCriteria[].criteriaWithSingleValue[].value#
        </iterate>
        <iterate conjunction="and" prepend="and" property="oredCriteria[].criteriaWithListValue">
          $oredCriteria[].criteriaWithListValue[].condition$
          <iterate close=")" conjunction="," open="(" property="oredCriteria[].criteriaWithListValue[].values">
            #oredCriteria[].criteriaWithListValue[].values[]#
          </iterate>
        </iterate>
        <iterate conjunction="and" prepend="and" property="oredCriteria[].criteriaWithBetweenValue">
          $oredCriteria[].criteriaWithBetweenValue[].condition$
          #oredCriteria[].criteriaWithBetweenValue[].values[0]# and
          #oredCriteria[].criteriaWithBetweenValue[].values[1]#
        </iterate>
        )
      </isEqual>
    </iterate>
  </sql>
  <sql id="oracle_Pagination_Head">
    <dynamic>
      <isNotEmpty property="oracleStart">
        <isNotEmpty property="oracleEnd">
          <![CDATA[select * from ( select row_.*, rownum rownum_ from ( ]]>
        </isNotEmpty>
      </isNotEmpty>
    </dynamic>
  </sql>
  <sql id="oracle_Pagination_Tail">
    <dynamic>
      <isNotEmpty property="oracleStart">
        <isNotEmpty property="oracleEnd">
          <![CDATA[) row_) where rownum_ > #oracleStart# and rownum_ <= #oracleEnd# and rownum <= ($oracleEnd$-$oracleStart$)]]>
        </isNotEmpty>
      </isNotEmpty>
    </dynamic>
  </sql>
<!--    
  <select id="dynamic_sql" resultClass="java.util.HashMap">
	    $sql$
  </select>
-->
  <select id="dynamic_sql_page" resultClass="java.util.HashMap">
  	<include refid="COMMON.oracle_Pagination_Head" />
	    $sql$
	<include refid="COMMON.oracle_Pagination_Tail" />
  </select>
<!--  
  <select id="dynamic_sql_count" resultClass="int">
  	select count(*) from ($sql$)
  </select>
-->
</sqlMap>